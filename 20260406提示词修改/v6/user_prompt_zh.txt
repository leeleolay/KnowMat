你将获得一篇经过OCR解析的材料科学论文文本内容。

=== 输入文本开始 ===
{paper_text}
=== 输入文本结束 ===

请执行以下信息提取工作流，严格遵守系统提示词的所有规则和统一的JSON数据模式 (Schema)：

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
步骤 1：全局元数据与数据角色鉴别 (Target vs Reference)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 从文本中提取论文标题（`Paper_Title`）和DOI（`DOI`）并置于根 `Paper_Metadata` 对象中。
- 【重要】：为了获取更丰满的材料数据，除了提取论文主体创制、测试的核心材料（Target）外，
  同一篇论文中引用的前人工作、标杆对比数据（Reference）也应当作为独立的数据项（Item）提取出来。
  你必须通过 `Composition_Info` 内的 `"Role": "Reference"` 标签做好来源隔离；本文自有成果标注为 `"Target"`。
  带有 "[18]" 或 "reported" 的数据一定是 Reference，绝不能混淆。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
步骤 2：数据项分解 (Item Decomposition)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 提炼所有不同的材料变体。任何（成分、元素改变）与（工艺状态、加载/打印方向）的差异，
  都必须在 `"items": []` 数组中创建一个新的独立对象。
- 【梯度材料】：如果是成分/组织沿某方向渐变的梯度材料，设置 `Gradient_Material: true`，
  成分用范围表示；若文献明确给出了每层的独立数据，才按层拆分为多个 Item，通过 `Gradient_Group_ID` 关联。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
步骤 3：成分提取 (`Composition_Info`)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 提取名义成分 (`Nominal_Composition`) 和实测成分 (`Measured_Composition`)，严格归一化至100%。
- 【组分表示格式】默认格式为 `"元素符号": 数值`，如 `"Ni": 53.0`、`"Fe": 18.5`。
  若有特殊成分标注（如范围值、上限标注、杂质说明等），在顶层 `Note` 字段中说明。
  示例：`"Note": "C含量≤0.08%，S含量≤0.015%，P含量≤0.015%，以上为杂质上限标注；Nb含量为5.0-5.5%范围值"`
- 遇总和小于100%且未指定余量元素，以 `"other"` 补全至100.0；若 `"other"` 为负则不添加。
  **注意**：成分提取的核心宗旨是**忠实反映文献提供的信息**，而非强行套用格式化规则。不要随意缩放或调整数值以满足100%归一化要求，唯一的例外是增加 `other` 元素。
- 商业成熟牌号（如 Inconel 718、316L SS）若未直接报告含量数字，保留 `Alloy_Name_Raw`，`Elements_Normalized` 设为 `null`。
- 若实测成分标注了检测方法（如"EDS"、"ICP"、"化学分析"），请填写 `Measurement_Method` 字段；若未提供则为 `null`。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
步骤 4：工艺提取 (`Process_Info`)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 双轨记录工艺描述文本（`original` 与 `simplified`）。
  **【重要】** `original` 字段必须**尽可能详细**地提取原文中的完整工艺描述，不做任何精简；`simplified` 字段保留关键信息并去除冗余，但不要过分简化。
- 若原文提及了具体设备型号或厂家信息，提取至 `Equipment` 字段（如 "EOS M290"、"真空感应熔炼炉"）；若无则填 `null`。
- 提取量化的变量至 `Key_Params`（使用标准命名，如 `Laser_Power_W`、`Solution_Temperature_K` 等）。
- 【热处理范围值】：如果温度或时间为范围（如"1000-1100°C"、"2-4h"），保留原始范围字符串，
  不要求平均值，例如 `"Solution_Temperature_K": "1273-1373"`。
- 若有多阶段热处理，必须后缀 `_Stage1_K`，严禁将不同阶段温度相加求均值。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
步骤 5：微观结构锁定 (`Microstructure_Info`)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 双轨生成 `Microstructure_Text`（`original` 和 `simplified`）。
  **【重要】** `original` 字段必须**尽可能详细**地提取原文中的完整微观组织描述，不做任何精简；`simplified` 字段保留关键信息并去除冗余，但不要过分简化。
- 【主相识别规则】：镍基/钴基/铁基合金中，主相（Main_Phase）应为 Gamma (γ) 基体或 FCC/BCC 等；
  析出相（Laves、MC/M₂C/M₆C Carbide、Gamma Prime (γ') 等）填入 `Precipitates[]` 数组。
  **绝对禁止**将 Laves 或 Carbides 识别为主相！
- 【相对密度】：必须严格按文献实际提供的数据填写；未提则填 `null`，
  **禁止**自行推断为 100%（这是幻觉行为）。
- 提取第1级/第2级量化参数（孔隙率、晶粒尺寸、析出相尺寸/体积分数、`Phase_Fraction_pct` 等）至根字段；
  高级物理参量（位错密度等）存入带单位的 `Advanced_Quantitative_Features`。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
步骤 6：物理力学性能采集 (`Properties_Info` — 极危易错步骤)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 将完全相同 Item 组合下的不同测试条件（不同温度、应变速率等）的力学结果
  全部压入 `Properties_Info` 列表，严格按第1级 -> 第2级 -> 第3级排序输出。
- 【延伸率三分法】：
  * `Elongation_Total`：总延伸率，文献中常标注为"Elongation"或"A"
  * `Elongation_Uniform`：均匀延伸率，颈缩前变形，可能标注为"A<sub>g</sub>"
  * `Elongation_At_Fracture`：断裂延伸率
  * 若原文仅给出一个"Elongation"而无细分，默认归入 `Elongation_Total`，并注明
- 【拉伸测试条件】：必须同步提取应变速率和拉伸速率
  * `Strain_Rate_s1`（应变速率，单位 s⁻¹）
  * `Tensile_Speed_mm_min`（拉伸速率，单位 mm/min）
- 【硬度测试条件】：必须同步提取加载力和保载时间
  * `Hardness_Load`（加载力，标注单位，如 "200gf"、"0.98N"）
  * `Hardness_Dwell_Time_s`（保载时间，单位秒，典型15s）
- 【温度错位查禁】：`Test_Temperature_K` 仅填写测试时的环境温度，
  严禁将加工/热处理温度填入。室温 = 298.15 K。
- 【拉压互斥】：受压测试场景下，所有属性名必须带 `_Compressive` 后缀，
  绝不可将拉伸指标用于压缩数据。
- 【格式规则】：
  * 范围值（如 200-230）：`Value_Numeric` = 范围均值，`Value_Range` = 原始范围字符串
  * 公差值（如 215±5）：`Value_Numeric` = 主值，`Value_StdDev` = 公差
  * 数据来源：文字表格填 `"text"`，图表图片填 `"image"`
- 【测试样本标准】：若文献提供了测试样本尺寸或符合的标准（如 ASTM E8、GB/T 228），
  在 `Test_Specimen` 字段标注。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
步骤 7：最终兜底检验门卡（强制自我审查）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
在输出 JSON 前，立即在内心执行以下审查：
- 【幻觉防控】：相对密度、组分等字段是否为 `null` 的地方被自行填了数据（尤其是100%）？
  任何文献未提供的数值都必须保持 `null`！
- 【引用污染辨识】：带 "[x]" 角标或出自引言/综述区的数据是否已标为 `Role: "Reference"`？
- 【遗漏追逃】：表格深处、正文一句带过的高温/低温/小变形量数据是否全部抓取？
- 【拉压混淆】：MPa 数值是否错误填入了 % 字段？拉伸和压缩属性是否张冠李戴？
- 【延伸率三分】：总延伸率、均匀延伸率、断裂延伸率是否已正确区分？

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
步骤 8：输出
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
- 验证根级 `Paper_Metadata` 和四个基础信息对象结构完备。
- 仅输出一个绝对合法的纯 JSON 代码串，
  绝对不允许在内容中带有普通对话闲聊文本或 Markdown 围栏提示号（如 ```）。
  输出从 `{"Paper_Metadata": ` 开始，到 `]}` 结束。
